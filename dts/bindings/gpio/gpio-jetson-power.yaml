description: GPIO pin attached to a sleep/wake pin

compatible: "gpio-jetson-power"

child-binding:
    description: "associated gpio pin"
    properties:
      gpios:
        type: phandle-array
        required: true
