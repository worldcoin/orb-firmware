description: STM32 PWM, but tfh vendor, as to not invoke the stm32 driver

compatible: "tfh,stm32-pwm"

properties:
  pinctrl-0:
    type: phandles
    required: true
  pinctrl-names:
    type: string
    required: false
  channels:
    type: array
    required: false
